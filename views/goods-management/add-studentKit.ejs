<!-- Content -->
<div class="content-area py-1">
    <div class="container-fluid">
        <!-- <h4>ثبت البسه محصل</h4>
        <ol class="breadcrumb no-bg mb-1">
            <li class="breadcrumb-item"><a href="index.html">خانه</a></li>
            <li class="breadcrumb-item"><a href="/goods-management">مدیریت جنسی</a></li>
            <li class="breadcrumb-item active">ثبت البسه محصل</li>
        </ol> -->

        <div class="box box-block bg-white">
            <!-- <h5 style="text-align: center;">ثبت البسه محصل در دیپو - یکشنبه 21 اسد سال 1402</h5> -->
            <br>
            <form action="/goods-management/add-studentKit" method="POST" class="<% if(typeof errors!==undefined){ errors.forEach(error=>{
                    if(error.name==='exist'){%>
                        has-danger
                    <%} }) } %> ">

                <div class="col-xs-5"></div>
                <% if(typeof errors!==undefined){ errors.forEach(error=>{
                        if(error.name==='exist'){ %>

                <div class="form-control-feedback">
                    <%= error.message %>
                </div>

                <% } }) } %>
                <br>

                <div class="form-group row<% if(typeof errors!==undefined){ errors.forEach(error=>{
				                if(error.name==='empty_stu'){%>
                    has-danger
                    <%} }) } %> ">
                    <div class="col-xs-3"></div>
                    <div class="col-xs-6">
                        <label for="example-text-input1" class="col-xs-3 col-form-label">آیدی ویا نمبرتذکره
                            محصل</label>
                        <div class="col-xs-9">
                            <div class="input-group">
                                <div class="col-xs-9">
                                    <input class="form-control" type="text" placeholder="آیدی ویا نمبر تذکره محصل"
                                        name="search" id="example-text-input1">
                                </div>
                                <button type="button" name="searchId" id="searchIds"
                                    class="col-xs-3 btn btn-primary fa fa-search">
                                    جستجو</button>
                                <% if(typeof errors!==undefined){ errors.forEach(error=>{
                                                if(error.name==='empty_stu'){ %>
                                <div class="form-control-feedback">
                                    <%= error.message %>
                                </div>
                                <% } }) } %>
                            </div>
                        </div>
                    </div>
                    <div class="col-xs-3">
                        <p id="result"></p>
                    </div>
                </div>

                <hr>
                <div class="form-group row">

                    <label for="example-text-input9" class="col-md-1 col-form-label">آیدی ویا نمبرتذکره محصل</label>
                    <div class="col-md-3<% if(typeof errors!== undefined){
                            errors.forEach(error=>{
                                if(error.name==='IdNumber'){%>
                                has-danger
                                 <%} }) } %>">
                        <input class="form-control" type="text" name="IdNumber" placeholder="آیدی ویا نمبرتذکره محصل"
                            id="example-text-input9" required>
                        <% if(typeof errors!==undefined){ errors.forEach(error=>{
							if(error.name==='IdNumber'){%>
                        <div class="form-control-feedback">
                            <%= error.message %>
                        </div>
                        <%} }) } %>
                    </div>

                    <label for="example-text-input5" class="col-md-1 col-form-label">دوشک</label>
                    <div class="col-md-3<% if(typeof errors!== undefined){
                    errors.forEach(error=>{
                        if(error.name==='mattress'){%>
                            has-danger
                    <%} }) } %>">
                        <input class="form-control" type="number" name="mattress" value="0" id="example-text-input5"
                            required>
                        <% if(typeof errors!==undefined){ errors.forEach(error=>{
							if(error.name==='mattress'){%>
                        <div class="form-control-feedback">
                            <%= error.message %>
                        </div>
                        <%} }) } %>
                    </div>
                    <label for="example-text-input9" class="col-md-1 col-form-label">بالشت</label>
                    <div class="col-md-3<% if(typeof errors!== undefined){
                    errors.forEach(error=>{
                        if(error.name==='pillow'){%>
                            has-danger
                    <%} }) } %>">
                        <input class="form-control" type="number" name="pillow" placeholder="بالشت" value="0"
                            id="example-text-input9" required>
                        <% if(typeof errors!==undefined){ errors.forEach(error=>{
							if(error.name==='pillow'){%>
                        <div class="form-control-feedback">
                            <%= error.message %>
                        </div>
                        <%} }) } %>
                    </div>
                </div>
                <div class="form-group row">
                    <label for="example-text-input10" class="col-md-1 col-form-label"> روجایی</label>
                    <div class="col-md-3<% if(typeof errors!== undefined){
                    errors.forEach(error=>{
                        if(error.name==='coverlet'){%>
                            has-danger
                    <%} }) } %>">
                        <input class="form-control" type="number" name="coverlet" value="0" placeholder="روجایی"
                            id="example-text-input10" required>
                        <% if(typeof errors!==undefined){ errors.forEach(error=>{
							if(error.name==='coverlet'){%>
                        <div class="form-control-feedback">
                            <%= error.message %>
                        </div>
                        <%} }) } %>
                    </div>
                    <label for="example-text-input5" class="col-md-1 col-form-label">کمپل</label>
                    <div class="col-md-3<% if(typeof errors!== undefined){
                    errors.forEach(error=>{
                        if(error.name==='quilt'){%>
                            has-danger
                    <%} }) } %>">
                        <input class="form-control" type="number" name="quilt" placeholder="کمپل" value="0"
                            id="example-text-input5" required>
                        <% if(typeof errors!==undefined){ errors.forEach(error=>{
							if(error.name==='quilt'){%>
                        <div class="form-control-feedback">
                            <%= error.message %>
                        </div>
                        <%} }) } %>
                    </div>
                    <label for="example-text-input9" class="col-md-1 col-form-label">چپرکت</label>
                    <div class="col-md-3<% if(typeof errors!== undefined){
                    errors.forEach(error=>{
                        if(error.name==='bed'){%>
                            has-danger
                    <%} }) } %>">
                        <input class="form-control" type="number" name="bed" value=0 placeholder="چپرکت"
                            id="example-text-input9" required>
                        <% if(typeof errors!==undefined){ errors.forEach(error=>{
							if(error.name==='bed'){%>
                        <div class="form-control-feedback">
                            <%= error.message %>
                        </div>
                        <%} }) } %>
                    </div>
                </div>
                <div class="form-group row">
                    <label for="example-text-input10" class="col-md-1 col-form-label"> آینه روی</label>
                    <div class="col-md-3<% if(typeof errors!== undefined){
                    errors.forEach(error=>{
                        if(error.name==='mirror'){%>
                            has-danger
                    <%} }) } %>">
                        <input class="form-control" type="number" name="mirror" value="0" placeholder="آینه روی"
                            id="example-text-input10" required>
                        <% if(typeof errors!==undefined){ errors.forEach(error=>{
							if(error.name==='mirror'){%>
                        <div class="form-control-feedback">
                            <%= error.message %>
                        </div>
                        <%} }) } %>
                    </div>
                    <label for="example-text-input5" class="col-md-1 col-form-label">پرده</label>
                    <div class="col-md-3<% if(typeof errors!== undefined){
                    errors.forEach(error=>{
                        if(error.name==='windowShade'){%>
                            has-danger
                    <%} }) } %>">
                        <input class="form-control" type="number" name="windowShade" value="0" placeholder="پرده"
                            id="example-text-input5" required>
                        <% if(typeof errors!==undefined){ errors.forEach(error=>{
							if(error.name==='windowShade'){%>
                        <div class="form-control-feedback">
                            <%= error.message %>
                        </div>
                        <%} }) } %>
                    </div>
                    <label for="example-text-input9" class="col-md-1 col-form-label">فرش</label>
                    <div class="col-md-3<% if(typeof errors!== undefined){
                    errors.forEach(error=>{
                        if(error.name==='carpet'){%>
                            has-danger
                    <%} }) } %>">
                        <input class="form-control" type="number" value="0" name="carpet" placeholder="فرش"
                            id="example-text-input9" required>
                        <% if(typeof errors!==undefined){ errors.forEach(error=>{
							if(error.name==='carpet'){%>
                        <div class="form-control-feedback">
                            <%= error.message %>
                        </div>
                        <%} }) } %>
                    </div>
                </div>

                <div class="form-group row">
                    <label for="example-text-input10" class="col-md-1 col-form-label"> میز</label>
                    <div class="col-md-3<% if(typeof errors!== undefined){
                    errors.forEach(error=>{
                        if(error.name==='table'){%>
                            has-danger
                    <%} }) } %>">
                        <input class="form-control" type="number" name="table" value="0" placeholder="میز"
                            id="example-text-input10" required>
                        <% if(typeof errors!==undefined){ errors.forEach(error=>{
							if(error.name==='table'){%>
                        <div class="form-control-feedback">
                            <%= error.message %>
                        </div>
                        <%} }) } %>
                    </div>
                    <label for="example-text-input5" class="col-md-1 col-form-label">چوکی</label>
                    <div class="col-md-3<% if(typeof errors!== undefined){
                    errors.forEach(error=>{
                        if(error.name==='chair'){%>
                            has-danger
                    <%} }) } %>">
                        <input class="form-control" type="number" name="chair" value="0" placeholder="چوکی"
                            id="example-text-input5" required>
                        <% if(typeof errors!==undefined){ errors.forEach(error=>{
							if(error.name==='chair'){%>
                        <div class="form-control-feedback">
                            <%= error.message %>
                        </div>
                        <%} }) } %>
                    </div>
                    <label for="example-text-input1" class="col-md-1 col-form-label">سال تحویلی جنس</label>
                    <div class="col-md-3">
                        <input class=" form-control" type="number" value="1401" name="Dates" id="example-text-input5"
                            required>
                    </div>
                </div>
                <div class=" form-group row">
                    <label for="example-text-input1" class="col-md-1 col-form-label">نام کتاب</label>

                    <div class="col-md-3 <% if(typeof errors!== undefined){
                        errors.forEach(error=>{
                            if(error.name==='nameBook'){%>
                            has-danger
                            <%} }) } %>">
                        <select name="nameBook" class="form-control">
                            <% bookName.forEach(b => { %>
                            <option value="<%= b %> "><%=b%></option>
                            <%})%>
                        </select>
                        <% if(typeof errors!==undefined){ errors.forEach(error=>{
                            if(error.name==='nameBook'){%>
                        <div class="form-control-feedback">
                            <%= error.message %>
                        </div>
                        <%} }) } %>
                    </div>
                    <label for="example-text-input1" class="col-md-1 col-form-label">جلد کتاب</label>
                    <div class="col-md-3 <% if(typeof errors!== undefined){
					errors.forEach(error=>{
						if(error.name==='skin'){%>
						has-danger
						<%} }) } %>">
                        <select name="skin" class="form-control">
                            <% skin.forEach(s => { %>
                            <option value="<%= s %> "><%=s%></option>
                            <%})%>
                        </select>

                        <% if(typeof errors!==undefined){ errors.forEach(error=>{
						if(error.name==='skin'){%>
                        <div class="form-control-feedback">
                            <%= error.message %>
                        </div>
                        <%} }) } %>
                    </div>

                    <label for="example-text-input1" class="col-md-1 col-form-label">صفحه کتاب</label>
                    <div class="col-md-3<% if(typeof errors!== undefined){
						errors.forEach(error=>{
						if(error.name==='notExit'){%>
						has-danger
						<%} }) } %>">
                        <select name="page" class="form-control">
                            <% page.forEach(p => { %>
                            <option value="<%= p %> "><%=p%></option>
                            <%})%>
                        </select>
                        <% if(typeof errors!==undefined){ errors.forEach(error=>{
							if(error.name==='notExit'){%>
                        <div class="form-control-feedback">
                            <%= error.message %>
                        </div>
                        <%} }) } %>
                    </div>

                </div>

                <div class="form-group row">


                    <label for="example-text-input1" class="col-md-1 col-form-label">سال کتاب</label>
                    <div class="col-md-3 <% if(typeof errors!== undefined){
					errors.forEach(error=>{
						if(error.name==='year'){%>
						has-danger
						<%} }) } %>">
                        <select name="year" class="form-control">
                            <% year.forEach(y => { %>
                            <option value="<%= y %> "><%=y%></option>
                            <%})%>
                        </select>
                        <% if(typeof errors!==undefined){ errors.forEach(error=>{
						if(error.name==='year'){%>
                        <div class="form-control-feedback">
                            <%= error.message %>
                        </div>
                        <%} }) } %>
                    </div>

                </div>
                <hr>
                <div class="form-group row">
                    <label for="example-number-input" class="col-xs-1 col-form-label">ذخیره
                        کردن</label>
                    <div class="col-xs-2">
                        <button type="submit" class="btn btn-primary">ذخیره</button>
                    </div>

                    <div class="col-xs-9"></div>
                </div>
            </form>
        </div>
    </div>
</div>
<script src="../../js/studentajax.js">

</script>